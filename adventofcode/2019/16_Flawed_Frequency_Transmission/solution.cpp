#include <bits/stdc++.h>
using namespace std;

static const vector BasePattern{0, 1, 0, -1};

constexpr int keep_last_digit(const int x) { return abs(x) % 10; }

int main() {
    string xs;
    cin >> xs;

    return 0;
}
