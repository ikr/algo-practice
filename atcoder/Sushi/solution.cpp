#include <bits/stdc++.h>
using namespace std;

double expected_operations_count(const vector<int> &xs) {
    // TODO
    return 0.0;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int sz = 0;
    cin >> sz;
    vector<int> xs(sz, 0);
    for (auto &x : xs) cin >> x;

    return 0;
}
