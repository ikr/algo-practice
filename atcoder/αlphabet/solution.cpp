#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    char c;
    cin >> c;
    if ('A' <= c && c <= 'Z') cout << 'A';
    else cout << 'a';
    cout << '\n';

    return 0;
}
