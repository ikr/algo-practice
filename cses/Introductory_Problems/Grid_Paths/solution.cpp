#include <bits/stdc++.h>
using namespace std;

int matching_paths_count(const string &ppattern) { return -1; }

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string s;
    cin >> s;

    return 0;
}
