#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    int facts_count;
    cin >> n >> facts_count;

    for (int i = 0; i != facts_count; ++i) {
        int t;
        int l;
        int r;
        cin >> t >> l >> r;
    }
}
